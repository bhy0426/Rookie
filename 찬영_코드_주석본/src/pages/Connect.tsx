export default function Connect() {
  // Connect는 /connect 주소에서 보여줄 페이지 컴포넌트입니다.
  // App.tsx의 <Route path="/connect" element={<Connect />} />와 연결됩니다.

  return <h1>Connect</h1>;
  // 아직 자세한 내용은 없고 제목만 표시합니다.
}
